
import {radioButton} from './modules/radio-button.js';
import {renderText} from './modules/render-text.js';

window.addEventListener('DOMContentLoaded', () => {
  window.addEventListener('load', () => {
    radioButton();
    renderText();
  });
});
